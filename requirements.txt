# Core PDF / image engine
pymupdf>=1.24        # PDF rasterization (carousel split)
Pillow>=10.0         # all image operations
pypdf>=4.0           # merge / compress / remove pages

# Desktop app (PDF Studio)
customtkinter>=5.2   # modern themed Tkinter UI
